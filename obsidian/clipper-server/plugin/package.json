{
  "name": "clipper-client",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit && node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "obsidian": "^1.7.2",
    "typescript": "^5.7.0"
  }
}
